{
  "id": "redis",
  "name": "Redis",
  "description": "使用 Rust 开发的 Redis 原生 IPC sidecar 驱动，为 Navop 提供二进制安全命令、流水线和有界 Pub/Sub 事件流，并可独立于主应用发布升级。 Rust native IPC sidecar driver for Navop with binary-safe commands, pipelines, bounded Pub/Sub event delivery, and an independent release lifecycle.",
  "api": "redis",
  "protocol_version": "1.0",
  "version": "0.1.5",
  "entry": { "command": "./redis-driver" },
  "transport": { "name": "redis.sock" },
  "methods": [
    "conn/open",
    "conn/close",
    "redis/command",
    "redis/pipeline",
    "event/open",
    "redis/pubsub_control",
    "event/read",
    "event/close"
  ],
  "compatibility": {
    "modes": ["standalone"],
    "server": { "min": "5.0" }
  },
  "ui": { "show_in_new_connection": false },
  "process": { "scope": "connection", "kill_on_drop": true }
}
